# Auto-loaded by `docker compose` alongside docker-compose.yml. Keeps local
# testing working without a real /etc/letsencrypt on the dev machine (only
# matters when ENABLE_SSL=true, which local testing never sets).
services:
  nginx:
    volumes:
      - static_data:/app/staticfiles:ro
      - media_data:/app/media:ro
